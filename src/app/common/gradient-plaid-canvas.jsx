import React from 'react'
import PropTypes from 'prop-types'

const GradientPlaidCanvas = ({ className }) => {
	return (
		<div className={`bg-granim ${className || ''}`} />
	)
}

export default GradientPlaidCanvas
